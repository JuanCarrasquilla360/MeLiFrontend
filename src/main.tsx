import React from 'react';
import ReactDOM from 'react-dom';
import './index.scss'; // Actualizado a SASS
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);